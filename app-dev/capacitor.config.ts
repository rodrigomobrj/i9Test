import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'i9-cirurgias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
